## 2026-03-27 — [RAZ-47] Theme toggle causing text selection
- Converted theme toggle from div to button element and added onMouseDown preventDefault
- Prevents rapid clicking from triggering text selection on "recently published" items

## 2026-03-26 — [RAZ-39] Article code block UI fixes
- Fixed language badge (e.g. "JSX") being too tall and crowding article text above by reducing padding/font-size and adding top margin
- Moved copy button inside the code content container so it no longer overflows on mobile

## 2026-03-26 — [RAZ-38] Article block scrollbar fix
- Removed max-h-[75vh] height constraint from code blocks so full content displays without vertical scrollbar
- Changed overflow-auto to overflow-x-auto to keep horizontal scroll for wide code but eliminate vertical scroll

## 2026-03-26 — [RAZ-32] Header padding fix v3 (mobile top spacing)
- Increased mobile vertical padding from 7px to 16px so logo isn't pressed against the top border
- Increased tablet container padding from 16px to 24px for better left spacing
- Previous fixes addressed desktop and mobile horizontal padding; this round fixes vertical spacing

## 2026-03-26 — [RAZ-31] Like button mobile v3 (drop shadow + overlay count)
- Removed ugly background container from mobile fixed like button
- Added drop-shadow to the heart SVG for visual separation
- Like count now overlaid in the center of the heart via a new `compact` prop on the Heart component
- Desktop sidebar layout unchanged

## 2026-03-26 — [RAZ-37] Render issue on article (list layout fix)
- Fixed article list items using display:flex which caused nested content (sub-paragraphs, ordered sub-lists) to render side-by-side in columns instead of stacking vertically
- Switched to position:relative + absolute ::before positioning for custom bullets/counters, allowing nested content to flow naturally

## 2026-03-26 — [RAZ-36] Title animation v2 + monster fade-in
- Revised back-button view transition: replaced manual document.startViewTransition() with router.replace() approach that intercepts popstate and uses ACTION_NAVIGATE path (same as Link clicks)
- Added 700ms opacity fade-in on homepage monster character to prevent jarring SVG jump-in on load

## 2026-03-26 — [RAZ-32] Header padding fix v2 (mobile/tablet)
- Previous fix only addressed desktop padding; mobile/tablet had px-0 on the sticky container
- Changed tablet:px-0 and max-tablet:px-0 to px-4 so mobile/tablet screens get proper left/right spacing

## 2026-03-26 — [RAZ-31] Like button fixed on mobile (v2)
- Made the heart/like button a standalone fixed-position element on mobile article pages
- Previous attempt using hidden + max-tablet:fixed on same element didn't reliably apply fixed positioning
- Now uses a separate div with `fixed bottom-6 right-6 z-50 tablet:hidden` outside the content flow

## 2026-03-25 — [RAZ-36] Title animation on browser back
- Fixed title view transition not playing when using browser back/forward buttons
- Root cause: Next.js popstate handler doesn't enable React's view transition context, so document.startViewTransition() was never called for back/forward navigation
- Added ViewTransitionOnBack component that intercepts popstate events and manually triggers the browser View Transitions API, plus explicit viewTransitionName inline styles on title elements

## 2026-03-25 — [RAZ-35] The article effect
- Added delayed fade-in animation (0.45s delay, 0.4s duration) to article content (tags, body, footer, sidebar)
- Title ViewTransition plays first, then content gracefully fades in with a subtle upward slide

## 2026-03-25 — [RAZ-33] Navbar items color fix
- Removed blanket `[&_a]:text-primary` from header that was making all nav links pink
- Moved pink color to only the logo link; menu items now use their own white (`text-text`) color

## 2026-03-25 — [RAZ-32] Header is missing padding
- Increased navbar sticky container horizontal padding from 50px to 80px
- Restores the left/right spacing that was present in the old design

## 2026-03-25 — [RAZ-31] Like button fixed on mobile
- Made the heart/like button fixed-positioned at bottom-right on mobile article pages
- Users can now like an article at any point while scrolling, not just at the end
- Uses existing `max-tablet` breakpoint; desktop sidebar layout unchanged

## 2026-03-25 — [RAZ-23] Add baseline Prisma migration for build-step support
- Created `prisma/migrations/0_init/migration.sql` baseline from existing schema (Article, UserLike, Subscriber)
- Marked as already applied via `prisma migrate resolve` so it won't re-run on the existing DB
- The build script (`prisma migrate deploy && next build`) now properly tracks schema changes going forward
- Future schema changes: run `npx prisma migrate dev --name <description>` locally, commit the generated migration, and it deploys automatically on build

## 2026-03-24 — [RAZ-25] Fix html font-size: 62.5% breaking Tailwind rem sizing
- Changed `html { font-size: 62.5% }` to `100%` and `body { font-size: 1.6rem }` to `16px` in globals.css
- The legacy 62.5% trick (1rem = 10px) was causing all Tailwind utility classes to render at 62.5% of intended size since Tailwind uses rem internally
- Code block rem values (0.8rem, 1.185rem, 0.7rem) left unchanged as they produce reasonable sizes at standard 16px base

## 2026-03-24 — [RAZ-9] Fix title animation snapping at end of transition
- Moved ViewTransition wrapper inside heading elements to wrap just the text span (inline-block), so the transition captures consistent bounding boxes instead of different container layouts.
- Added custom CSS for ::view-transition-group/old/new pseudo-elements: 0.5s duration with smooth cubic-bezier easing to eliminate the end-of-animation snap.
- The snap was caused by the h3 (left-aligned, 22px) and h1 (centered, 38px) having different container layouts that the view transition couldn't smoothly interpolate.

## 2026-03-24 — [RAZ-9] Fix title animation using React ViewTransition component
- Replaced inline `viewTransitionName` CSS with React's `<ViewTransition>` component (from React 19) in both BlogList and ArticlePage.
- The inline CSS approach didn't work because Next.js's `experimental.viewTransition` integrates with React's ViewTransition component, not raw CSS properties. React's `<ViewTransition>` properly hooks into `document.startViewTransition()` during navigations.

## 2026-03-24 — [RAZ-9] Fix crypto.randomUUID crash on non-secure contexts
- Added a Math.random-based UUID v4 fallback in HeartLike's getUserId() for browsers where crypto.randomUUID is unavailable (HTTP, older browsers).
- The original crash was introduced in RAZ-8 when `uuid` package was replaced with `crypto.randomUUID()`.

## 2026-03-24 — [RAZ-7] Unit tests for likes API logic
- Extracted getLikes/addLike business logic from route handler into `src/lib/likes.ts` for testability.
- Added vitest with 6 tests covering: existing articles, missing articles (auto-creation), user like counts, article upsert, and increment behavior.
- Route handler (`src/app/api/likes/route.ts`) now delegates to the extracted functions.
- Added `npm test` script to package.json.

## 2026-03-23 — [RAZ-7] Like button DB investigation
- Found and fixed a double-count bug: first like on a new article was counted twice in UserLike because the article upsert's create branch also created a UserLike, then the separate userLike upsert incremented it again.
- Created missing Article, UserLike, and Subscriber tables in the database (they had never been migrated).
- Verified end-to-end: likes are now correctly stored and incremented in PostgreSQL.
- Note: production deployment on Coolify should ideally use a dedicated database for the blog, not the shared Paperclip platform DB.

## 2026-03-23 — [RAZ-10] Dark mode transition improvement
- Added universal CSS transitions (color, background-color, border-color, box-shadow, fill, stroke) at 350ms to `*, ::before, ::after` in GlobalStyles.
- Previously only `body` and `h1-h6` had transitions, causing inconsistent speed when toggling themes.
- All elements now transition uniformly for a smooth dark mode switch.

## 2026-03-23 — [RAZ-6] Issue creation auth fix
- Researched CORS/auth issue with report-issue feature. The server-side proxy architecture was correct.
- CEO updated route.ts with session-based auth using email/password login to Paperclip API.
- Env vars needed in Coolify: PAPERCLIP_API_URL, PAPERCLIP_PUBLIC_URL, PAPERCLIP_EMAIL, PAPERCLIP_PASSWORD, PAPERCLIP_COMPANY_ID, PAPERCLIP_PROJECT_ID.

## 2026-03-23 — [RAZ-5] Report issue password localStorage
- Feature was already implemented. Password saved to localStorage on successful submission and loaded on mount.

## 2026-03-23 — [RAZ-9] Title animation between homepage and article
- Implemented using the CSS View Transitions API with Next.js experimental `viewTransition` support.
- Added matching `viewTransitionName` on article titles in BlogList (homepage) and ArticlePage (article).
- The browser animates the title from its list position to the article header (and back) during navigation.
- Uses only browser APIs — no JS animation libraries needed.

## 2026-03-23 — [RAZ-8] Page speed performance improvements
- Replaced `uuid` with native `crypto.randomUUID()` (~4KB bundle savings)
- Replaced `date-fns` with native `Intl.DateTimeFormat` (~13KB bundle savings)
- Throttled StickMan eye-tracking mouse listener with `requestAnimationFrame` (reduces CPU)
- Added `stale-while-revalidate` cache headers to likes API GET endpoint
- All content pages already static (SSG) with `generateStaticParams` — no changes needed
- Next.js `Link` already prefetches article pages on viewport entry in production
- Known follow-up: styled-components runtime could be replaced with CSS Modules for further gains

## 2026-03-24 — [RAZ-12] Set up Tailwind CSS and convert theme system to CSS variables
- Installed tailwindcss v3.4, postcss, autoprefixer
- Created tailwind.config.ts with custom theme mapping all theme.ts colors/breakpoints to CSS variables
- Created postcss.config.js for Tailwind/Autoprefixer PostCSS pipeline
- Created src/app/globals.css with CSS custom properties for dark (default) and light themes, Tailwind directives, and all global reset styles from GlobalStyles.tsx
- Updated ThemeContext.tsx to toggle `light` class on `<html>` for CSS-variable-based dark mode (class strategy), while keeping styled-components ThemeProvider for backward compatibility during migration
- Updated layout.tsx to import globals.css and removed GlobalStyles component
- StyledComponentsRegistry kept for now — existing components still use styled-components and will be migrated in subsequent tasks (RAZ-13 through RAZ-16)

## 2026-03-24 — [RAZ-13 through RAZ-22] Complete styled-components to Tailwind migration
- Migrated all 10 component files from styled-components to Tailwind CSS utility classes
- Components: Header, Homepage/StickMan, ArticlePage, BlogList, Projects, Snippets, HeartLike, Code, MdxComponents, ReportIssue, About, ThemeIcons, ThoughtCloud, VideoGifImg
- Complex styles (article content, code syntax tokens, thought cloud pseudo-elements, sticky note transforms) moved to CSS classes in globals.css
- Dynamic styles (puzzle piece fills, variant colors, drag states) use inline styles or conditional classes
- Animations (updown, slide, heartbeat, icon-enter, fade-in) added as keyframes in tailwind.config.ts
- Removed styled-components, @types/styled-components, StyledComponentsRegistry, GlobalStyles, theme.ts, styled.d.ts
- ThemeContext simplified to CSS-class-based theming only (no more SCThemeProvider)
- Build + all 6 tests pass with zero regressions

## 2026-03-24 — [RAZ-26] Fix missing Snippets sticky-note rotation transforms
- The nth-child CSS selectors on `.sticky-note` never matched because each div was wrapped in a `<Link>` parent
- Added `.snippets-grid` class to the container and changed CSS selectors to `.snippets-grid > :nth-child(n) .sticky-note`

## 2026-03-24 — [RAZ-27] Fix Header max-width inconsistency
- Changed Header max-width from 1300px to 1200px to match the layout container width

## 2026-03-24 — [RAZ-28] Fix missing Wotfard-Bold font file
- Mapped font-weight 600 to Wotfard-SemiBold.woff2 instead of non-existent Wotfard-Bold.woff2

## 2026-03-25 — [RAZ-30] Replace emoji icons with SVG icons in info panels
- Replaced Unicode emoji icons (ℹ️, ⚠️, ✅, 🛑) with inline SVG icons for all TextBlock variants (Info, Warning, Success, Danger, Primary)
- The info icon now uses the classic "i in a circle" SVG matching the original design
- SVG icons inherit color from the variant's border color for consistent theming
